/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'com.homeocare.app',
  appName: 'Homeocare',
  webDir: 'dist',
  bundledWebRuntime: false
};

module.exports = config;
